# =============================================================================
# Distributed training / orchestration extras. Split out of requirements.txt
# 2026-05-27 because Ray + Kubernetes + KFP add ~1 GB to the default image
# while only being relevant to multi-node workflows.
#
# Use this when you need Ray on a notebook stack:
#   REQUIREMENTS_FILE=requirements-distributed.txt \
#       docker compose -p dist up -d --build
#
# This file is *additive* to the default ML stack — include all the deps
# you need from requirements.txt too, or maintain a separate base profile.
# Pinning policy mirrors requirements.txt (R3: >=X.Y,<Z.0).
# =============================================================================

ray[data,train,tune,serve]>=2.54,<3.0
kubernetes>=31.0,<32.0
kfp-pipeline-spec>=0.5,<3.0
kfp-server-api>=2.4,<3.0
grpcio-health-checking>=1.68,<2.0
